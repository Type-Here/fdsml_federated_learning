# ============================================================
# Training / inference environment: NVIDIA GPU machine.
#
# Everything in requirements_dev.txt, plus the deep-learning stack and the
# extra dependencies introduced by this project (Part A: FedDisco/FIPA,
# Part B: GTSRB-C test-time adaptation).
#
# Install (Python 3.11, in this order):
#   1) pip install -r requirements_gpu.txt
#   2) pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# torch/torchvision are installed SEPARATELY (step 2) on purpose: the CUDA
# wheels do not live on PyPI, so they cannot be pinned here.
#
# ------------------------------------------------------------
# ON GOOGLE COLAB: do NOT install this file as-is.
# Colab ships its own torch + numpy; re-pinning numpy==1.26.4 breaks the
# preinstalled CUDA torch. Install only what is missing:
#   !pip install phe==1.5.0 gmpy2==2.3.1 flask-socketio==5.1.1 python-socketio==5.3.0 \
#                eventlet==0.35.2 imagecorruptions thop
# and leave numpy / torch / torchvision / scikit-learn alone.
# ------------------------------------------------------------
# ============================================================

# --- numerical / data ---
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.5.0
scipy==1.13.1
matplotlib==3.9.0
pillow==10.3.0
tqdm==4.66.4

# --- partially homomorphic encryption (Paillier) ---
phe==1.5.0
# Optional for `phe`, but not optional for us: without it every modular
# exponentiation runs in pure Python. Measured on the encrypted FIPA round
# (p*r ciphertext multiplications per client), it is an 8.6x difference -
# 135 s against 16 s for a 4-client round on the ResNet18 head. The server
# does that work inside a Socket.IO handler, so it is also how long the
# server stops answering its clients.
gmpy2==2.3.1

# --- web server & socket stack ---
Flask==2.0.3
Werkzeug==2.0.3
Flask-SocketIO==5.1.1
python-socketio==5.3.0
python-engineio==4.9.1
simple-websocket==1.0.0
eventlet==0.35.2
websocket-client==1.8.0
requests==2.32.3

# --- template / CLI helpers pulled in by Flask ---
Jinja2==3.1.3
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.7
bidict==0.23.1

# --- testing ---
pytest==8.2.2

# ============================================================
# Added by this project (not in the codebase we received)
# ============================================================

# Part B (M4) - GTSRB-C corruption benchmark.
# Left UNPINNED on purpose: it drags in scikit-image, and pinning that
# conflicts with the numpy==1.26.4 pin above. Known caveats:
#   - `glass_blur` fails with scikit-image >= 0.23 (the `multichannel`
#     argument was removed). Exclude it from the corruption set, or
#     reimplement it with cv2 / scipy.ndimage.
#   - `fog` fails on numpy >= 2.0 (`np.float_` removed) but WORKS with the
#     numpy==1.26.4 pinned here.
# Verify the corruption set FIRST (WP4.1) before building on top of it.
imagecorruptions

# Part B (M4/WP4.6) - forward MAC counting for the edge-efficiency table.
thop==0.1.1.post2209072238

# --- PyTorch CUDA: install SEPARATELY, see header ---
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126