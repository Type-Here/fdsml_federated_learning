# ============================================================
# Local development environment (no GPU, no torch).
#
# Purpose: edit + unit-test the parts of the project that do NOT need a
# neural network: dataset preparation, DatasetSplitter (Dirichlet), the
# FedDisco weighting math, and the Paillier (PHE) helpers in utils.py.
#
# Training and inference run elsewhere (Colab or an NVIDIA machine):
# see requirements_gpu.txt.
#
# Install:
#   uv venv --python 3.11 .venv
#   .venv/bin/python -m pip install -r requirements_dev.txt
#
# NOTE on Python version: the project pins numpy==1.26.4 / scikit-learn==1.5.0
# and an old Flask stack (2.0.3). Those have no wheels for Python 3.13/3.14.
# Use Python 3.11.
# ============================================================

# --- numerical / data ---
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.5.0
scipy==1.13.1
matplotlib==3.9.0
pillow==10.3.0
tqdm==4.66.4

# --- partially homomorphic encryption (Paillier) ---
phe==1.5.0
# Optional for `phe`, but not optional for us: without it every modular
# exponentiation runs in pure Python. Measured on the encrypted FIPA round
# (p*r ciphertext multiplications per client), it is an 8.6x difference -
# 135 s against 16 s for a 4-client round on the ResNet18 head.
gmpy2==2.3.1

# --- web server & socket stack (needed to import federated_server / trusted_authority) ---
Flask==2.0.3
Werkzeug==2.0.3
Flask-SocketIO==5.1.1
python-socketio==5.3.0
python-engineio==4.9.1
simple-websocket==1.0.0
eventlet==0.35.2
websocket-client==1.8.0
requests==2.32.3

# --- template / CLI helpers pulled in by Flask ---
Jinja2==3.1.3
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.7
bidict==0.23.1

# --- testing ---
pytest==8.2.2

# Deliberately NOT here (see requirements_gpu.txt): torch, torchvision,
# imagecorruptions, thop.
#
# Dropped from the original requirements_linux.txt because nothing in the
# codebase imports them: socketIO-client (abandoned 2016 package, unrelated
# to `python-socketio`), seaborn.
